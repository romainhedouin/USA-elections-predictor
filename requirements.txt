# The pipeline and the site need only this.
requests==2.32.3

# Needed only by the superseded scraper in legacy/ — safe to omit if you are
# not running it (a deployment does not need them).
beautifulsoup4==4.12.3
selenium==4.26.1
